{{ n >= 1 }}

best = 1;
max = -1;
i = 1;
while (i <= n) {
    a[i] ~ lap(e/2) + q[i];
    if (a[i] > max) then {
        best = i;
        max = a[i];
    }
    i = i + 1;
}

{{ for j . ((j >= 1) & ( j <= n)) => (a[best] > q[j] - (4/e) * log(n/beta)) }}
{{ beta }}